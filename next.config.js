/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the experimental section if it only contained appDir
  // If you have other experimental features, keep them but remove appDir
  experimental: {
    // other experimental features can stay here
  },
  // other config options...
}

module.exports = nextConfig
